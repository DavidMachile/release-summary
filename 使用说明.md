# release-summary 使用说明

> 从 Git 提交历史自动生成发版变更摘要，支持 AI 智能总结。

## 安装

```bash
curl -sSL https://raw.githubusercontent.com/DavidMachile/release-summary/master/install.sh | bash
```

安装完成后，确保 `~/bin` 在 PATH 中（如果提示的话）：

```bash
echo 'export PATH="$HOME/bin:$PATH"' >> ~/.zshrc
source ~/.zshrc
```

**依赖**: git（必需）、Claude CLI（可选，用于 AI 总结）

## 基本用法

```bash
# 两个 commit 之间的变更
release-summary abc1234 def5678

# 某个 commit 到当前 HEAD
release-summary abc1234

# 查看版本
release-summary --version

# 查看帮助
release-summary --help
```

## 高级用法

```bash
# 指定项目名称
release-summary -n "我的项目" abc1234 def5678

# 指定输出文件路径
release-summary -o changelog/v2.0.0.md abc1234 def5678

# 组合使用
release-summary -n "MyApp" -o release/v1.2.0.md abc1234 def5678
```

## 选项说明

| 选项 | 说明 |
|------|------|
| `-n, --name` | 项目名称，默认取仓库目录名 |
| `-o, --output` | 输出文件路径，默认 `RELEASE_SUMMARY.md` |
| `-v, --version` | 显示版本号 |
| `-h, --help` | 显示帮助信息 |

## 输出示例

```markdown
# MyApp Release Summary

> **Generated**: 2026-05-26
> **Range**: `abc1234 .. def5678`
> **Commits**: 12

## Highlights

**Auth Module**
- Added OAuth2 login support with Google and GitHub providers
- Fixed token refresh race condition on session expiry

**Payment**
- Integrated Stripe checkout for subscription billing

## Commits

def5678 feat: add OAuth2 login with Google provider (Jane)
abc1234 fix: token refresh race condition (Mike)
...
```

## AI 总结

如果系统已安装 [Claude CLI](https://docs.anthropic.com/en/docs/claude-code)，脚本会自动调用 AI 生成 **Highlights** 分组总结。没有 Claude CLI 也能正常使用，只输出 commit 列表。

## 卸载

```bash
rm "$(which release-summary)"
```
